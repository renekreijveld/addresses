meta {
  name: Post Address
  type: http
  seq: 6
}

post {
  url: {{API_ROOT}}/api/v1/addresses
  body: json
  auth: bearer
}

auth:bearer {
  token: {{API_TOKEN}}
}

body:json {
  {"catid": "0", "state": "1", "title": "Testadres", "address": "Teststraat 1", "postcode": "1234 ZZ", "city": "Amsterdam", "country": "Nederland" }
}

settings {
  encodeUrl: true
}
