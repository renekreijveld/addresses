meta {
  name: Del Address
  type: http
  seq: 7
}

delete {
  url: {{API_ROOT}}/api/v1/addresses
  body: json
  auth: bearer
}

auth:bearer {
  token: {{API_TOKEN}}
}

body:json {
  {"id": "25" }
}

settings {
  encodeUrl: true
}
